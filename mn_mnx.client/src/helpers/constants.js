const apiUrl = "/api/"

const objects = {
    generic: "Generic",
}

const reactObject = {
    public: "public",
    user: "user",
    post: "post"
}

const publicTypes = {
    user: "user"
}

const userTypes = {
    list: "list",
    form: "form",
    profile: "profile",
    profileSettings: "profileSettings"
}

const postTypes = {
    list: "list",
    form: "form"
}

export {
    apiUrl,
    objects,
    reactObject,

    publicTypes,
    userTypes,
    postTypes
}