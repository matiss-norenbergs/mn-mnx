const apiUrl = "/api/"

const objects = {
    user: "UserData",
    auth: "Authentication",
    generic: "Generic",
    post: "PostData"
}

const reactObject = {
    user: "user",
    post: "post"
}

const userTypes = {
    list: "list",
    form: "form"
}

const postTypes = {
    list: "list",
    form: "form"
}

export {
    apiUrl,
    objects,
    reactObject,

    userTypes,
    postTypes
}